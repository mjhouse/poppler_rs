# poppler_rs
Wrapper for the Poppler PDF rendering library.

### Getting started

```bash
sudo apt install -y     \
    libpoppler-glib-dev \
    libpoppler-dev      \
    libcairo2-dev       \
    libglib2.0-dev
```